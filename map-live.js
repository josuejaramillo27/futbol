import{initializeApp,getApps}from'https://www.gstatic.com/firebasejs/10.8.0/firebase-app.js';import{getFirestore,collection,getDocs}from'https://www.gstatic.com/firebasejs/10.8.0/firebase-firestore.js';const cfg={apiKey:'AIzaSyBqZSb3ZkI1QqoLGyP47cdD7eexwdStdXK',authDomain:'app-futbol-acd0f.firebaseapp.com',projectId:'app-futbol-acd0f',storageBucket:'app-futbol-acd0f.firebasestorage.app',messagingSenderId:'223446110165',appId:'1:223446110165:web:219afce6a9dac03203f75c'},app=getApps().length?getApps()[0]:initializeApp(cfg),db=getFirestore(app),el=document.getElementById('map-canchas');if(el&&window.L){const map=L.map(el,{zoomControl:false}).setView([-5.1945,-80.6328],13);L.control.zoom({position:'bottomright'}).addTo(map);L.tileLayer('https://{s}.basemaps.cartocdn.com/rastertiles/voyager/{z}/{x}/{y}{r}.png',{subdomains:'abcd',maxZoom:20,attribution:'&copy; OpenStreetMap &copy; CARTO'}).addTo(map);const markers=L.layerGroup().addTo(map),status=document.getElementById('map-status'),btn=document.getElementById('btn-mi-ubicacion'),icon=L.divIcon({className:'premium-map-marker',html:'<div><i class="ph-fill ph-soccer-ball"></i></div>',iconSize:[40,40],iconAnchor:[20,38]});const pos=c=>{let a=Number(c.lat),b=Number(c.lng);if(Number.isFinite(a)&&Number.isFinite(b))return[a,b];const u=String(c.ubicacionLink||''),m=u.match(/@(-?\d+(?:\.\d+)?),(-?\d+(?:\.\d+)?)/);return m?[Number(m[1]),Number(m[2])]:null};async function load(){try{const[a,b]=await Promise.all([getDocs(collection(db,'canchas')),getDocs(collection(db,'espacios'))]),owners=new Map(),list=[];a.forEach(x=>owners.set(x.id,{id:x.id,...x.data()}));a.forEach(x=>{
      const c={id:x.id,...x.data()};
      // FASE 10: Filtro de publicación en mapas en vivo
      if(c.estadoPublicacion === 'published' || (c.configurado === true && !c.estadoPublicacion)) list.push(c);
  });if(c.configurado!==false)list.push(c)});b.forEach(x=>{const s={id:x.id,...x.data()},p=owners.get(s.ownerUid);if(p)list.push({...p,...s,id:x.id,nombre:s.nombre||p.nombre,tipoCancha:s.tipo||p.tipoCancha,precio:s.precio??p.precio})});const bounds=[];list.forEach(c=>{const p=pos(c);if(!p)return;const m=L.marker(p,{icon}).bindPopup(`<b>${String(c.nombre||'Cancha').replace(/[<>]/g,'')}</b><br><small>${String(c.ubicacionTexto||c.distrito||'Ubicación disponible').replace(/[<>]/g,'')}</small>`);m.on('click',()=>window.abrirModal?.(c.id));m.addTo(markers);bounds.push(p)});if(bounds.length){map.fitBounds(bounds,{padding:[35,35],maxZoom:14});status.textContent=`${bounds.length} cancha${bounds.length===1?'':'s'} ubicada${bounds.length===1?'':'s'} en el mapa.`}else status.textContent='Aún no hay canchas con ubicación en el mapa.';setTimeout(()=>map.invalidateSize(true),150)}catch(e){console.error(e);status.textContent='No pudimos cargar el mapa.'}}btn?.addEventListener('click',()=>navigator.geolocation?.getCurrentPosition(p=>{map.setView([p.coords.latitude,p.coords.longitude],14);L.circleMarker([p.coords.latitude,p.coords.longitude],{radius:7}).addTo(map)}));load();window.addEventListener('resize',()=>map.invalidateSize(true))}
